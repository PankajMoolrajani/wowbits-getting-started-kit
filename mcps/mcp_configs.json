{
    "mcps": [
        {
            "name": "shodan",
            "config": {
                "api_key": "your_api_key_here",
                "transport_mode": "http"
            }
        },
        {
            "name": "llm-sandbox",
            "config": {
                "backend": "docker",
                "transport_mode": "stdio"
            }
        }
    ]
}